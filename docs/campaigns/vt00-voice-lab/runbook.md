# VT00 Voice Lab operator runbook

Status: `DRAFT — DEPLOYMENT AND CERTIFICATION PENDING`

This runbook operates the isolated Voice Lab test plane against the ordinary deployed Sophia browser voice path. It does not authorize a general production test mode, direct provider substitution, text injection in place of audio, or use of a person's voice as fallback.

The machine-readable deployment order, environment ownership, rollback points, cleanup exceptions, and quantitative gate state live in `deployment-gates.yaml`. Update that checkpoint from `unpassed` only from attached execution evidence.

## Roles and stop authority

- The campaign operator owns deployment identity, secret entry, spend bounds, and the kill switch.
- The test agent uses only the installed private plugin for certification journeys.
- A security or privacy operator approves any raw-audio exception. Raw audio is off by default.
- Anyone observing credential exposure, an unexpected principal, an unbounded provider resource, or orphan cleanup failure stops the campaign immediately.

## Deployment record

Fill this before opening any gate:

| Field | Required value | Observed value |
|---|---|---|
| Repository branch | `codex/sophia-observability-v1` | `PENDING` |
| Bootstrap candidate A commit SHA | exact committed pre-registration SHA | `PENDING` |
| Final candidate B commit SHA | exact committed packaging-finalization SHA | `PENDING` |
| Frontend URL / final B build SHA / closed-final, campaign-open, and campaign-close Vercel deploy IDs | `https://www.sophia-ei.com` / exact B SHA / three distinct manually promoted deploys | `PENDING` |
| Gateway URL / final B build SHA | `https://sophia-gateway.onrender.com` / exact B SHA | `PENDING` |
| LangGraph URL / final B build SHA / deploy ID | assigned Render URL / exact B SHA / exact deploy | `PENDING` |
| Voice URL / final B build SHA | `https://sophia-voice-2uzr.onrender.com` / exact B SHA | `PENDING` |
| Voice Lab MCP URL / final B build SHA | assigned Render URL / exact B SHA | `PENDING` |
| Voice Lab worker bootstrap/final/open/close deploy attestations | exact A/B SHA, deploy ID, one live boot/instance hash, deployment-identity hash, effective gate, and heartbeat after both worker-deploy start and current MCP boot | `PENDING` |
| Bootstrap plugin version / package SHA-256 | exact A manifest SemVer / exact A tree hash | `PENDING` |
| Registered app ID / final plugin version / package SHA-256 | real `plugin_asdk_app…` / exact helper-emitted `+codex.<sanitized-token>` SemVer / exact B tree hash | `PENDING` |
| Isolated Postgres | `sophia-voice-lab-postgres` from database-only Blueprint | `PENDING` |

The committed candidate pins `sophia-voice-lab-mcp.onrender.com` as one identity across the host allowlist, OAuth issuer/resource/metadata URLs, plugin README, and diagnostic `.mcp.json`. If Render assigns any other hostname, hard-stop instead of changing only `SOPHIA_VOICE_LAB_ALLOWED_HOSTS`: update every coupled URL and exact origin, rerun the package/tests, recompute the pre-registration package hash, and commit/push a replacement candidate A before continuing. Never use a wildcard or register a hostname that differs from the committed package.

## Provisioning

1. Review `render.yaml`, `render.voice-lab.database.yaml`, and `render.voice-lab.yaml`. Pin the three Render Blueprint sources to `codex/sophia-observability-v1` with custom paths `render.yaml`, `render.voice-lab.database.yaml`, and `render.voice-lab.yaml` respectively; set Auto Sync to No for each. The database Blueprint owns only the isolated Postgres instance. The Voice Lab service Blueprint owns only its two services and references the already-created database by exact name. Never manage one resource from multiple Blueprints. Confirm every Render service targets the required branch, both Voice Lab services use the same Dockerfile and root build context, and all five Render services declare `autoDeployTrigger: off`.
2. Disable Git-triggered production deployment for the existing LangGraph, Gateway, and Voice Render services, set the linked main Render Blueprint's **Auto Sync** to **No**, and pause automatic production deployment of the target branch in Vercel. Attest those live settings plus the `autoDeployTrigger: off` declarations in `render.yaml`; keep both service auto-deploy and Blueprint auto-sync disabled through the ordered candidate-B rollout, and do not manually sync the main Blueprint before the D02 migration.
3. Generate and stage the following distinct values in the approved secret manager without printing them. Install values on existing product services only at their ordered deployment, and install Voice Lab values only when the service Blueprint is created from committed candidate A:
   - `SOPHIA_VOICE_LAB_OAUTH_CONSENT_SECRET` and `SOPHIA_VOICE_LAB_OAUTH_TOKEN_PEPPER` for the registered app;
   - `SOPHIA_VOICE_LAB_BEARER_TOKEN` for diagnostic preflight only;
   - `SOPHIA_VOICE_LAB_FAULT_BEARER_TOKEN` for a separately authorized destructive-fault diagnostic lane;
   - `SOPHIA_VOICE_LAB_PRINCIPAL_ID` for the dedicated synthetic principal;
   - the exact matching `SOPHIA_VOICE_LAB_TEST_PRINCIPAL`, `SOPHIA_VOICE_LAB_TEST_EMAIL`, and stable `SOPHIA_VOICE_LAB_TEST_NAME` on the protected frontend; use the same principal identity on every owning product service and never reuse an ordinary account;
   - `SOPHIA_VOICE_LAB_PROVISION_OPERATOR_BEARER_TOKEN` on the Voice Lab web service and only in the one-time provision operator shell; never mount it on the worker, browser, registered app, or deployment controller;
   - mutually distinct frontend grant, runtime capability, recovery, and service-internal secrets matching their owning product boundaries;
   - a versioned `SOPHIA_VOICE_LAB_CALLER_PARTITION_KEYS_JSON` keyring on both Voice Lab services, with one active key and retained prior keys until every row carrying the prior partition ID has passed its terminal retention boundary;
   - three offline Ed25519 attestation keypairs and distinct web-only transport tokens generated with the exact `init` command in `tools/sophia-voice-lab/scripts/external-attestations/README.md`; install `SOPHIA_VOICE_LAB_ATTESTATION_PUBLIC_KEYS_JSON` on web and worker and `SOPHIA_VOICE_LAB_ATTESTATION_TRANSPORT_TOKENS_JSON` on web only, while every private key and its matching transport token remain with the independent source controller;
   - one versioned `SOPHIA_VOICE_LAB_AUTH_TOMBSTONE_ACTIVE_KID` and canonical `SOPHIA_VOICE_LAB_AUTH_TOMBSTONE_KEYS` keyring on the protected frontend and Gateway; retain every prior key until its last grant, cleanup obligation, and audit retention window has expired, and never substitute the legacy single-secret fallback in production;
   - the restricted product-auth cleanup runtime `SOPHIA_VOICE_LAB_AUTH_DATABASE_URL` on LangGraph and Gateway only, targeting the same product database while remaining distinct from both the owner migration DSN and the execute-only D02 Gateway DSN;
   - `SOPHIA_VOICE_LAB_D02_GATEWAY_CAPABILITY_SECRET` on the Voice Lab web service and Gateway only;
   - `SOPHIA_VOICE_LAB_D02_DATABASE_FINALIZE_HMAC_KEY_ID` and `SOPHIA_VOICE_LAB_D02_DATABASE_FINALIZE_HMAC_SECRET` on the Gateway and the migration operator only; use the initial key ID `d02-db-finalize-v1`, manage the rotatable key ID in the Gateway dashboard rather than the repository Blueprint, and never copy this authority to Voice Lab web, Voice, the worker, MCP, browser, or controller. Generate the finalize secret independently and require it to differ from every grant, capability, recovery, service-internal, Builder, settlement-identity, tombstone-keyring, OAuth consent/pepper, observability, backend-token, and private signing authority in scope without printing or importing those other values into the operator;
   - `SOPHIA_VOICE_LAB_D02_SETTLEMENT_IDENTITY_HMAC_SECRET` and the D02 settlement Ed25519 private key on the Gateway only; and
   - the matching current D02 settlement public key, authority key ID, and retained public-key keyring on the Gateway and Voice Lab web service only. Never mount any D02 capability, HMAC, or private signing authority on the browser worker, MCP caller, or deployment controller.
4. Commit and push the completed pre-registration source to `codex/sophia-observability-v1` as bootstrap candidate A before syncing either Voice Lab Blueprint or executing source-dependent product DDL. Record the exact remote commit SHA and run every operator command below from that clean, exact checkout; an uncommitted migration, role operator, or Blueprint is forbidden. A push-triggered deployment before the D02 migration is a hard stop.
5. From exact candidate A, create/sync the database-only Blueprint using exact source branch `codex/sophia-observability-v1` and custom path `render.voice-lab.database.yaml` in the intended Render production environment; leave its Auto Sync disabled. Wait for `sophia-voice-lab-postgres` to become available before creating either service. Do not sync the service Blueprint yet. Do not modify or sync the repository's main `render.yaml` for the isolated plane.
6. Confirm the isolated Render database `sophia-voice-lab-postgres` has no public IP allowlist entries and is not shared with ordinary Sophia state. That database is only harness/MCP/worker state. Never create the D02 Gateway role or apply the auth/cleanup migrations there.
7. Target the existing product/Better-Auth PostgreSQL database selected by the restricted `BETTER_AUTH_DATABASE_URL` and the exact `BETTER_AUTH_EXPECTED_SUPABASE_PROJECT_REF`. Using a separate `postgres` owner DSN for that same database, run the audited create-only role operator from `frontend/` before applying the Voice Lab migrations:

   ```bash
   export SOPHIA_VOICE_LAB_D02_ROLE_PROVISION_APPROVED=YES
   export SOPHIA_VOICE_LAB_ENVIRONMENT=production
   export SOPHIA_VOICE_LAB_AUTH_MIGRATION_DATABASE_URL='postgresql://<owner>@<product-auth-host>/<database>'
   export SOPHIA_VOICE_LAB_D02_GATEWAY_DATABASE_PASSWORD='<new-distinct-runtime-password>'
   export BETTER_AUTH_EXPECTED_SUPABASE_PROJECT_REF='<exact-project-ref>'
   pnpm auth:voice-lab-d02-role:provision
   unset SOPHIA_VOICE_LAB_D02_ROLE_PROVISION_APPROVED \
     SOPHIA_VOICE_LAB_D02_GATEWAY_DATABASE_PASSWORD
   ```

   The command must prove the owner target before DDL, create only the fixed `sophia_voice_lab_gateway` login, verify the supplied credential by logging in as that exact role, and output only hashed/safe identity. It must hard-stop on an existing drifted role, wrong preserved credential, effective authority inherited through `PUBLIC`, or authority in any non-system application schema. The role must be `LOGIN`, `NOINHERIT`, `NOSUPERUSER`, `NOCREATEDB`, `NOCREATEROLE`, `NOREPLICATION`, and `NOBYPASSRLS`, have no inbound or outbound role memberships, and have no `CREATE` privilege on schema `public`. Store its credential only in `SOPHIA_VOICE_LAB_D02_GATEWAY_DATABASE_URL`; do not reuse the owner, frontend, service-role, or general Gateway DSN.

   On hosted Supabase PostgreSQL 17, `CREATE ROLE` can add one automatic creator-membership row granted by the inaccessible platform role `supabase_admin`. The customer `postgres` owner cannot revoke a grant owned by that role. Do not weaken the zero-membership contract or continue to migration. If rollback-only production probes have established this exact condition and identified only redundant `PUBLIC EXECUTE` on `postgres`-owned, non-extension routines in schema `public`, repeat the command once with `SOPHIA_VOICE_LAB_D02_SUPABASE_SUPPORT_PREPARE_APPROVED=YES`. That explicit transaction first revokes only those redundant `PUBLIC` routine grants while preserving every explicit role grant and excluding extension-owned objects; any remaining effective application authority rolls the transaction back. Supabase-managed `auth`, `extensions`, `graphql`, `graphql_public`, `net`, `pgbouncer`, `realtime`, `storage`, `supabase_functions`, and `vault` namespaces are platform schemas rather than Sophia application schemas: platform-owned object ACLs remain unchanged, but the operator separately requires both `USAGE` and `CREATE` to be denied on every present platform namespace, making those raw ACLs unreachable. Every other non-system schema retains the stricter schema plus raw-object denial. The transaction may then commit only an otherwise-exact role carrying exactly one `(granted_role=sophia_voice_lab_gateway, member=postgres, grantor=supabase_admin, admin=true, inherit=false, set=false)` row; it still proves the credential and every direct/effective application-authority denial, returns `ok:false` with `support_required:true`, and exits nonzero. Ask Supabase Support to remove the exact platform-owned creator rows for both `better_auth_app` and `sophia_voice_lab_gateway`. Do not send either password. After Support confirms removal, unset the preparation approval and rerun the ordinary command. It must return `ok:true`, `membership_attested:true`, and `support_required:false` before step 8. Any other role edge, attribute, owner, grantor, option, platform namespace access, or effective application-object path is a hard stop. Extension-owned objects remain governed by their installed extension ACLs and do not count as Voice Lab application authority.
8. Still from `frontend/`, export `BETTER_AUTH_DATABASE_URL`, `SOPHIA_VOICE_LAB_AUTH_MIGRATION_DATABASE_URL`, `BETTER_AUTH_EXPECTED_SUPABASE_PROJECT_REF`, `SOPHIA_VOICE_LAB_D02_DATABASE_FINALIZE_HMAC_KEY_ID`, and `SOPHIA_VOICE_LAB_D02_DATABASE_FINALIZE_HMAC_SECRET`, then run `pnpm auth:voice-lab-ledger:verify-files`, `pnpm auth:voice-lab-ledger:apply`, and `pnpm auth:voice-lab-ledger:preflight`. The first command is database-free; apply performs the locked pre-DDL fresh/predecessor and role checks, and normal preflight is only for the installed current schema. Apply with the PostgreSQL owner credential, never with a restricted runtime DSN. The operator sets the authority values transaction-locally with parameterized `set_config`, compares only the secret SHA in postflight, and must never print the secret or retain its GUC after commit/rollback. Require the operator to reject an absent or drifted D02 role before DDL, then require exact schema/catalog/owner/constraint/index/trigger/foreign-key/ACL/function/prosrc attestation after DDL. The `better_auth_app` runtime must retain its pre-existing CRUD on the four Better Auth `user`, `session`, `account`, and `verification` tables and have exactly CRUD on the Voice Lab auth-grant, cleanup-obligation, and cleanup-admission tables; `SELECT, UPDATE` on `sophia_sessions` and cleanup scan cursors; no access to session messages or artifact records; and non-grantable `EXECUTE` only on the D02 `sources-zero` boolean readback. It must have no schema creation, cursor insert/delete, other D02 routine, or raw D02 table/column/sequence authority. A partially existing or self-sealed drifted schema is a hard stop. Immediately after preflight, clear every operator-only value from the shell:

   ```bash
   unset BETTER_AUTH_DATABASE_URL \
     BETTER_AUTH_EXPECTED_SUPABASE_PROJECT_REF \
     SOPHIA_VOICE_LAB_AUTH_MIGRATION_DATABASE_URL \
     SOPHIA_VOICE_LAB_D02_ROLE_PROVISION_APPROVED \
     SOPHIA_VOICE_LAB_D02_GATEWAY_DATABASE_PASSWORD \
     SOPHIA_VOICE_LAB_ENVIRONMENT \
     SOPHIA_VOICE_LAB_D02_DATABASE_FINALIZE_HMAC_KEY_ID \
     SOPHIA_VOICE_LAB_D02_DATABASE_FINALIZE_HMAC_SECRET \
     SOPHIA_VOICE_LAB_D02_FINALIZE_AUTHORITY_ROTATION_APPROVED
   ```
9. Confirm through the migration operator's restricted-role postflight that the D02 runtime role has zero raw table, view, materialized-view, foreign-table, column, or sequence privileges on application objects in schema `public`. Its only application-object authority in `public` is non-grantable `EXECUTE` on the 15 exact operation-scoped functions: finalize-authority readiness, freeze authorize/finalize, provider-freeze, producer-open, sources-zero, Voice-terminal authorize/finalize, settlement authorize/finalize, relay begin/refresh/end, and continuity authorize/finalize. It must not execute the six canonical-JSON/HMAC/proof/browser-settlement/capability-ledger internal helpers. During role provisioning, separately deny `USAGE` and object privileges on every other non-system application schema. The live Gateway connection performs its independent runtime attestation only after candidate A is deployed in step 11.
   - The migration removes PostgreSQL's built-in global default `PUBLIC` execute grant and any D02 Gateway default grants for future `postgres`-owned functions. A schema-specific revoke cannot override the global default, so this targeted denial intentionally covers every schema. Existing defaults for unrelated, non-member service roles are preserved and remain subject to their own service review. Do not restore the global `PUBLIC` default or grant default authority to the D02 Gateway role.
10. Generate the D02 Ed25519 keypair out of band. The authority key ID identifies exactly the current SPKI; the retained public-key keyring must include the current entry and every prior verification key until the last receipt signed by that key has passed its retention and purge deadline. Rotate by adding the new public key before switching the private key, then retain the old public key until that deadline. Never retain or distribute an old private key. Database-finalize HMAC rotation is deliberately different and permits no overlapping authority: hold the campaign kill switch, drain every Gateway replica, and prove there is no active/pending D02 freeze, relay, continuity observation, or mutation. Set a new dashboard-managed Gateway `SOPHIA_VOICE_LAB_D02_DATABASE_FINALIZE_HMAC_KEY_ID` and secret, set `SOPHIA_VOICE_LAB_D02_FINALIZE_AUTHORITY_ROTATION_APPROVED=YES` only in the operator shell, record the old and new key IDs only (never either secret), and run `pnpm exec node scripts/migrate-voice-lab-auth-ledger.mjs --apply --rotate-d02-finalize-authority`. The operator refuses a same-key ID or secret fingerprint, missing approval, open kill switch, relay, unsettled freeze, or incomplete continuity chain. Deploy only Gateway replicas carrying those exact dashboard values; a later Blueprint sync must not restore the initial key ID. Require `finalize_authority_ready` and Gateway `/ready` before reopening, and prove every old pod/proof fails closed immediately. Any non-quiescent rotation is forbidden.
11. Before the exact-A Vercel build, set the protected frontend `SOPHIA_VOICE_LAB_PROVISIONING_ENABLED=true` while keeping Voice Lab disabled and its product kill switch engaged. Then manually deploy/promote exact bootstrap candidate A to Vercel and manually sync the main Render Blueprint at exact A only after the D02 migration is green; keep every automatic deployment setting disabled. The protected product plane includes `sophia-langgraph`, frontend, Gateway, and Voice. Put the exact dedicated principal value on LangGraph as well as the other owning services so offline/Builder exclusion is active before client allocation. Require LangGraph `/ok` and its content-free `/version`, signed frontend readiness with `voice_lab_enabled:false`, `kill_switch_engaged:true`, `provisioning_enabled:true`, the recorded Vercel deploy ID, and the exact A build, Gateway `/version` plus `/ready`, and Voice `/version` plus `/ready` to match A. While the product gates remain closed, require Gateway `voice_lab_admission_ready:true` (the independent retention/bootstrap fence), Gateway `voice_lab_mutation_ready:false`, and Voice `voice_lab_mutation_ready:false`. Do not proceed from a stale LangGraph or frontend deploy even if the three-field product capability identity matches.
12. Validate the candidate-A pre-registration plugin and compute its current tree hash as documented in `plugins/sophia-voice-lab/PACKAGING.md`. Record its exact manifest version and tree hash as the bootstrap package identity. Create/sync the service-only Blueprint from branch `codex/sophia-observability-v1` and custom path `render.voice-lab.yaml`, with Auto Sync disabled, only after the database and protected product plane are ready. On both services set `SOPHIA_VOICE_LAB_REPOSITORY_CANDIDATE_SHA` and every expected product/dependency SHA to A, set `SOPHIA_VOICE_LAB_PLUGIN_VERSION` to the exact bootstrap manifest version, supply the bootstrap package hash, and leave `SOPHIA_VOICE_LAB_REGISTERED_APP_ID` blank. Set both service-scoped kill switches to `true`, set the Voice Lab web `SOPHIA_VOICE_LAB_PROVISIONING_ENABLED=true`, and provide the web-only provision-operator bearer; the protected frontend must already report its matching true gate from step 11. Render may create both services during one Blueprint sync, but the operator gate is ordered: first record the worker deploy ID/start and prove exactly one live exact-A worker heartbeat whose effective kill switch is `true`, boot and instance hashes are unique, deployment-identity hash is current, observation follows the worker deploy start, and fixture/browser readiness is true; then accept the MCP web deployment and require a subsequent heartbeat newer than that MCP boot. The web may report HTTP 200 `provisioning_required` only while both kill switches are engaged and settled, both provisioning gates are enabled, every deployment/database/OAuth/worker check is healthy, and the signed frontend response is the exact unprovisioned projection.
13. Provision the dedicated principal exactly once through the audited MCP operator endpoint. The operator shell receives the scoped provision bearer, never `SOPHIA_VOICE_LAB_GRANT_SECRET`; the MCP service owns capability minting and writes the durable global audit. Use one newly generated idempotency key and retain it unchanged for response-loss replay:

   ```bash
   export SOPHIA_VOICE_LAB_PRINCIPAL_PROVISION_APPROVED=YES
   export SOPHIA_VOICE_LAB_PROVISIONING_ENABLED=true
   export SOPHIA_VOICE_LAB_ENVIRONMENT=production
   export SOPHIA_VOICE_LAB_TARGET_MCP_URL='https://<exact-mcp-host>'
   export SOPHIA_VOICE_LAB_PROVISION_OPERATOR_BEARER_TOKEN='<web-only-operator-bearer>'
   export SOPHIA_VOICE_LAB_PRINCIPAL_PROVISION_IDEMPOTENCY_KEY='<new-random-safe-id>'
   export SOPHIA_VOICE_LAB_EXPECTED_FRONTEND_SHA='<bootstrap-candidate-a-sha>'
   export SOPHIA_VOICE_LAB_EXPECTED_BACKEND_SHA='<bootstrap-candidate-a-sha>'
   export SOPHIA_VOICE_LAB_EXPECTED_VOICE_SHA='<bootstrap-candidate-a-sha>'
   export SOPHIA_VOICE_LAB_EXPECTED_MCP_SHA='<bootstrap-candidate-a-sha>'
   pnpm auth:voice-lab-principal:provision
   unset SOPHIA_VOICE_LAB_PRINCIPAL_PROVISION_APPROVED \
     SOPHIA_VOICE_LAB_PROVISIONING_ENABLED \
     SOPHIA_VOICE_LAB_ENVIRONMENT \
     SOPHIA_VOICE_LAB_TARGET_MCP_URL \
     SOPHIA_VOICE_LAB_PROVISION_OPERATOR_BEARER_TOKEN \
     SOPHIA_VOICE_LAB_PRINCIPAL_PROVISION_IDEMPOTENCY_KEY \
     SOPHIA_VOICE_LAB_EXPECTED_FRONTEND_SHA \
     SOPHIA_VOICE_LAB_EXPECTED_BACKEND_SHA \
     SOPHIA_VOICE_LAB_EXPECTED_VOICE_SHA \
     SOPHIA_VOICE_LAB_EXPECTED_MCP_SHA
   ```

   Require the receipt to bind the expected and observed frontend/MCP builds, request/capability/principal hashes, and the durable audit identity. Immediately set `SOPHIA_VOICE_LAB_PROVISIONING_ENABLED=false` on both the protected frontend and Voice Lab web service. With automatic deployment still paused, manually redeploy/promote the protected frontend from exact A and redeploy the Voice Lab web service from exact A; re-attest both build identities and require normal MCP `ready` rather than `provisioning_required`. Exact same-key replay may return the immutable receipt; a different key must not create another principal or audit identity.
14. Confirm the Gateway `/ready` probe attests the dedicated D02 login session, every application-schema denial, zero raw relation/column/sequence authority, the exact function catalog, and database-finalize authority while Voice Lab remains disabled. Confirm MCP `/version` and `/readyz`, the exact worker boot/instance/deployment-identity hashes, settled `true` worker gate, post-MCP-boot heartbeat observation, and all product/dependency build identities still report bootstrap candidate A. A healthy endpoint is registration infrastructure only, not a campaign pass.
15. Register that exact kill-switched candidate-A MCP HTTPS endpoint as the private remote app and obtain the real `plugin_asdk_app...` technical identity. Do not use a fabricated bootstrap ID. Add the exact `.app.json` mapping and manifest `apps` field, update the plugin cachebuster through the plugin-creator helper, validate, and compute the final package hash twice. The resulting manifest version must be strict SemVer with exactly one helper-emitted `+codex.<sanitized-lowercase-token>` suffix; the default token is the 14-digit UTC timestamp. Commit those packaging-finalization bytes to `codex/sophia-observability-v1` as final candidate B, push it, and require the target branch to resolve to exact B. An uncommitted registration overlay is forbidden.
16. Keep all kill switches engaged and all product flags disabled. Manually deploy/promote exact candidate B to Vercel, record that closed-final Vercel deploy ID, manually sync the main Render Blueprint at exact B, and redeploy Voice Lab worker and MCP from exact B while automatic deployment remains disabled. Require signed frontend readiness from that exact deploy to report the B build, `voice_lab_enabled:false`, `kill_switch_engaged:true`, and `provisioning_enabled:false`; a prior exact-B deploy with different environment state is stale and cannot satisfy the gate. Set `SOPHIA_VOICE_LAB_REPOSITORY_CANDIDATE_SHA`, the four expected product/dependency SHAs, the final plugin version, the final package hash, and the real registered app ID on both Voice Lab services before accepting their final B readiness. Deploy the worker before the MCP web service, record both deploy IDs/start times, then require all six build identities to report B and capability/readiness to echo the final version/hash/app identity. Require exactly one live final worker boot, effective kill switch `true`, the final deployment-identity hash, and an observation after both the worker deploy start and current MCP boot. Candidate A, the bootstrap hash, and the bootstrap version can never satisfy P01.
17. Install the final candidate-B plugin from the repository marketplace only after every B deployment is verified. Collect the official platform installation/fresh-task evidence and keep all mutation gates closed until every preflight below passes.

## Preflight before enabling mutations

- Verify the frontend, Gateway, Voice, and separately bound LangGraph dependency identities from their public identity endpoints, then verify the MCP and worker candidate identities. Record every SHA and deploy ID above. Require the exact Gateway/Voice mutation-gate projection to be internally consistent; closed product gates remain bootstrap-healthy, but MCP admission, API start admission, and worker browser/provider allocation must each independently require both product mutation gates open. LangGraph remains outside the strict three-field frontend/backend/voice capability identity, but its `/version` receipt is mandatory Builder-plane admission evidence.
- Confirm the frontend, gateway, voice service, MCP service, and worker share the expected capability secret without displaying it.
- Confirm the dedicated principal is not an ordinary user and cannot write memories, learning, offline work, notifications, recap, or analytics state.
- Enumerate every frontend `/api` and Gateway method/template. Prove the dedicated principal is default-denied before body parsing, provider/client/storage allocation, or raw backend-ticket issuance except for the exact governed Voice Lab/session/transcript/Builder evidence allowlist; prove unauthenticated global/mutable endpoints reject and ordinary authenticated controls remain unchanged.
- With analytics consent deliberately enabled in the imported browser state, prove the HttpOnly synthetic context causes every early-page and session telemetry event to be dropped or isolated before the ordinary analytics sink. Exercise direct error reporting and breadcrumbs before connect, while active, and after terminal cleanup; prove zero synthetic Sentry allocation and prove an ordinary control event is unchanged.
- Validate OAuth discovery/protected-resource metadata and exercise missing, expired, wrong-resource, wrong-scope, revoked/replayed, and malformed client credentials plus an invalid diagnostic fault credential. Every case must fail before a browser or provider-bearing session is created.
- Validate the independent attestation lane with a key that is unavailable to MCP callers and browser workers. Reject generic outcome assertions: D02 must bind actual before/after deployment and process receipts, A03 must bind a real lost-response/replay client transcript to the immutable operation and owning effects, and P01 must bind the platform-authored registered app, private install, fresh task, and bounded call audit.
- Exercise D02 with the restricted `sophia_voice_lab_gateway` connection, including an immutable `before_api_restart` product-continuity observation, the exact one-shot restart, the chained `after_api_restart` observation, freeze, provider-event relay, event-stream relay, settlement, and byte-identical response-loss replay for every receipt. Prove the before/after continuity projections are identical and uniquely chained to one restart request; a freeze is durable before the single Render mutation, blocks continuation and completion, binds the exact current and potentially live provider epochs, and cannot settle until canonical browser terminal receipts cover the frozen epoch set, the owning Voice instance has durably terminalized every frozen epoch, and every Gateway relay is authoritatively drained. For browser-worker termination, also prove one globally consumed Voice Lab dispatch claim exists between the durable command/freeze and the sole Render POST; replay, ambiguous response, a distinct attempt, and two copied valid journal prefixes must produce zero additional Render POSTs. A lease timeout or a different replica's local absence is not terminal proof.
- Fault the D02 controller at every journal boundary. Resume only the exact persisted termination ID and signed command/final claims; require the Render mutation count to remain exactly one. An ambiguous Render response may be reconciled only with read-only provider state and must stop for operator review when the exact result cannot be proven.
- Confirm run, operation, browser lease, and capture bounds match the Blueprint.
- Confirm raw audio remains disabled and the evidence retention window is approved.
- Confirm the local hard-deadline purge and the product-owned cross-restart/cross-deploy expiry reaper are live under both the product and runner kill switches. Prove the signed opaque cleanup obligation is present and immutable in every session, message, finalization, provider, Builder, artifact, and authentication projection. Prove a finalization committed before the provisional deadline establishes the final retention window, while an at-or-after-deadline promotion is rejected under the same cleanup barrier and cannot publish a receipt. Force a timed-out lease acquisition and prove no local or PostgreSQL advisory lock survives; seed more than one discovery batch of malformed/conflicting rows and prove cursor/quarantine progress still reaches and purges later valid obligations without blocking the event loop. Exercise a rolling-shutdown crash at every prepared-handle boundary, a store outage through the handle SLO, and a concurrent late write; require overdue readiness degradation while the identity-free handle remains actionable, immediate common-index zero before `complete`, and final handle deletion/read-zero only after the completed tombstone is durable.
- Confirm the private plugin's `.app.json` points to the newly registered `plugin_asdk_app` technical ID, its remote app points to the newly verified MCP HTTPS endpoint, and neither bundle contains a secret. A `.mcp.json` preflight connection is not final installation evidence.
- Exercise a worst-case bounded `end_voice_run` through the registered app and prove the platform keeps the call open through terminal finalization. The diagnostic `.mcp.json` timeout is 180 seconds; current OpenAI plugin documentation does not publish a registered-app tool timeout, so only an installed-surface execution can close this gate.

Record every result as `PASS`, `FAIL`, or `PENDING`. Do not open mutations while any required item is `FAIL` or `PENDING`.

The current Gateway active-session ownership is keyed by the single dedicated principal. Keep global and per-caller concurrency at one and execute regression-suite children sequentially. Parallel runs are forbidden until the product-plane ownership model changes and gains its own evidence-backed gate.

## Opening and closing the campaign window

1. While Voice Lab web admission remains closed, set the protected frontend test-plane enable flag only for the dedicated principal and open its product-plane kill switch, then manually redeploy/promote the frontend from exact candidate B. Record the new campaign-open Vercel deploy ID and require signed frontend readiness from that exact deployment to report the B build, `voice_lab_enabled:true`, `kill_switch_engaged:false`, and `provisioning_enabled:false`; neither the closed-final B deploy nor any unpromoted preview is admissible. Set and deploy the matching Gateway/Voice flags and product kill switches. Before touching the worker, require exact current-B Gateway and Voice deploy IDs plus `voice_lab_enabled:true`, `voice_lab_kill_switch_engaged:false`, and `voice_lab_mutation_ready:true` from both public readiness projections; Gateway must also retain `voice_lab_admission_ready:true`.
2. Set the service-scoped Voice Lab worker execution kill switch to `false`, record the resulting Render worker deploy ID/start, and wait for exactly one live current-B heartbeat from a new boot. Require effective kill switch `false`, the open-state deployment-identity hash, fixture/browser readiness, and `observed_at` after both that deploy start and the current MCP boot. Only then set the separately stored Voice Lab MCP web-admission kill switch to `false`; require the redeployed web `/readyz` to report `execution_gate_settled:true`, both product mutation gates open, and `mutation_ready:true`. Keep destructive socket rotation protected by explicit `voice_lab:fault` scope and the distinct diagnostic fault bearer.
3. Record start time, operator, intended scenario IDs, maximum run count, and maximum spend.
4. Run the scenario manifest through a fresh Codex task using the installed plugin. Certification must not use repository-local commands, raw JavaScript, manual browser takeover, or a human microphone.
5. On completion or the first stop condition, close the Voice Lab MCP web-admission kill switch first and verify the new web deploy reports mutations closed while remaining health-readable. Complete exact owned cleanup and terminal-zero proof, then set the worker execution kill switch to `true`, record the resulting deploy ID/start, and require exactly one new current-B worker boot whose effective gate is `true`, deployment-identity hash is the closed-state hash, and heartbeat follows both the worker deploy start and current MCP boot; `/readyz` must then report `execution_gate_settled:true`. Close the Gateway/Voice product-plane kill switches and disable their flags. Last, set the protected frontend kill switch to engaged and its enable flag to disabled, manually redeploy/promote exact candidate B, record the new campaign-close Vercel deploy ID, and require signed readiness from that exact deployment to report the B build, `voice_lab_enabled:false`, `kill_switch_engaged:true`, and `provisioning_enabled:false`. The campaign is not closed on an environment-variable edit alone or on readiness from the campaign-open deploy.

## Required autonomous flow

For each run, retain the structured tool response and cursor:

1. Read capabilities and exact limits.
2. Start a run with exact frontend, gateway, and voice deployment SHAs.
3. Speak through deterministic fixtures or governed TTS and require a page-side schedule receipt.
4. Wait for input PCM/transcription, assistant transcription, output received, playback scheduled/started/completed, canonical turns, and any tool/task/UI projection as separate facts.
5. Exercise barge-in only relative to a cited playback-start receipt.
6. Exercise socket rotation only with the latest cited provider epoch and require a restored or typed degraded result.
7. End through the ordinary product UI and wait for bounded finalization and cleanup evidence.
8. Export the durable evidence manifest after browser shutdown and verify every artifact hash.

## Failure handling

- Classify authorization, deployment mismatch, harness, product, provider, evidence, and cleanup failures separately.
- Preserve failure evidence before repair. Never relabel a product failure as a harness pass or infer playback from transcription/provider audio.
- On capture gap, generation mismatch, missing required join, or unavailable durable manifest, mark the harness invalid/failed and rerun only after repair.
- On suspected secret exposure, revoke the OAuth family and diagnostic bearers, then rotate every affected consent/token, grant, capability, recovery, or internal-auth secret before any further run.
- On orphan browser/provider/task state, engage the kill switch, disable product-plane enable flags, terminate the owned resource, and record the cleanup receipt.

## Rollback and closeout

- Engage all kill switches first.
- Roll the two Voice Lab services back together to the last verified image, or suspend them. Do not roll only the web or worker when schemas differ.
- Preserve the isolated database until evidence hashes and the campaign report are reconciled; then apply the approved retention action.
- Confirm zero active runs, browser leases, synthetic sessions, provider sessions, and owned tasks.
- Complete `campaign-report-template.md`. Deployment, certification, and cleanup remain `PENDING` until evidence references are attached.
