# A-008 calibrated validation plan (prepared, not executed)

The preceding three-run window is closed as diagnosed. It demonstrated no product repair. C5's earlier acceptance remains historical. J6's provider French transcription has no proven physical cause; its timing archive indicates degraded delivery on the retained timestamps. R1 and R3 had timely 71-frame input delivery, but neither produced a first-turn response. R3's End returned 503 after finalization, and Lab recovery settled live resources; recovery is not a normal End.

## Worker profiles and admission

The checked-in Render Blueprint retains `starter` as the idle and retention-maintenance default. On 2026-09-24 the live Starter worker's read-only cgroup files reported `cpu.max=50000 100000` and `memory.max=536870912`: 0.5 CPU and 512 MiB. The new active-run fence classifies this as insufficient. Before an authorized validation, upsize **the same worker** to Pro 2 CPU/4 GB, verify cgroup CPU quota at least 2 and memory limit at least 3.5 GiB in the live readiness/heartbeat, and keep one instance and the existing environment, source and retention duties. A Blueprint sync with `plan: 2c-4g` would make that larger plan the declared default; this Blueprint intentionally does not. After the run is terminal, no leases remain, and retention deadlines are safe, revert the same worker to Starter with kill switch engaged. No sizing or gate mutation is authorized by this plan alone.

The active-run start checks the *served* frontend's signed `auth:readiness` response, including `control_adapter_enabled`, before allocating provider resources. Its timeout is 15 seconds. In production, `ensureBetterAuthSchema()` returns without DDL; the route reads ledger/principal state under transactions and a principal advisory lock, and does not provision an account or session. A disabled adapter returns `FRONTEND_CONTROL_ADAPTER_DISABLED`. The worker profile is measured from cgroup files, not inferred from Blueprint text.

Input delivery uses browser `_capture_provenance.observed_at` for PCM frame and context event timing. Missing or malformed browser time makes the test invalid; explicit ledger-time fallback is reserved for historical replay. A speech gap above 150 ms, lag growth above 100 ms, or context/wall ratio error above 5% yields `harness=invalid_test` and `product=unavailable`. The J6 archive was purged before provenance could be compared, so its timing replay is fallback evidence. Read-only Postgres comparison for R1 and R3 found all 71 frame browser timestamps present and exactly equal to their stored event timestamps (0 ms maximum difference). Their CSV timing verdicts are browser-time evidence.

## One ordinary-app validation, after review and authority

1. Review and land the separate W1 input-validity, W2 End integrity, and W3 microphone-resampler PRs with their exact candidate checks. Confirm deployed frontend, Gateway, Voice, LangGraph and Lab identities; do not assume a branch SHA is deployed.
2. Reconcile the actual incremental cost of the preceding window. The current AI Studio €0.17 and Render Lab worker $5.77 / Lab MCP $5.18 are month-to-date values that include pre-window baseline; they are not campaign charges. The prior conservative three-run projection was $4.86 against a $5 ceiling, leaving only $0.14 in that projection. No new paid run fits a defensible cap from these figures alone.
3. With explicit authorization for deployment, temporary worker sizing, gates and one bounded run, verify one live worker at the active profile, kill switch state, clean leases, signed served frontend adapter preflight, correct deployment identities, and the retained R1/R3 purge deadlines. Keep the automatic retention worker available until purge proof.
4. In the ordinary app, perform **one** non-Builder conversation run with the pinned natural `conversation_greeting_probe`, then the pinned exact J6-text `conversation_calm_probe`, at most two utterances and the existing 15-second clip/900-second run limits. Observe PCM receipts, browser-time delivery, provider input transcript, reply, supported empty/nonempty End response, canonical data and cleanup separately. Abort on mismatched deployment, invalid input timing, lost provenance, worker profile loss, failed adapter preflight, or stale retention proof.
5. Export the redacted evidence and report only the scoped product observation established by that run. An invalid input test has no product verdict. A recovered End remains labeled recovery. This single validation is not VT00 certification.

## Consolidated authority request, if the candidate passes review

Request approval once for: deployment of the three exact reviewed candidate commits; a temporary upsize of the existing Lab worker to Pro 2 CPU/4 GB and return to Starter; the minimum temporary gate changes needed for the ordinary-app run; **one** two-utterance paid validation under a newly stated incremental spending ceiling; and automatic cleanup/retention maintenance. Supply the measured window cost and proposed ceiling before seeking that approval. No current instruction resets the $5 cap or authorizes another provider run.
