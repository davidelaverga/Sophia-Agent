# C2 text-model candidate — not activated

Parent e59136cb08d430d91bbc1e9c10f181f2196d9e7c. This selects existing isolated
C2 integration, not the dirty C1 worktree. No deployment, profile activation,
provider change or additional migration is performed by this commit.

## Selected boundary

Companion/Builder factories use run-owned provider clients and a final physical
request authority callback. Context entry/readmission reconstructs safe recorded
input, excludes stale retained carriers and seals admitted model continuations.
The transport issues fresh single-use final admission for each physical request;
parsed model results are joined separately. Fallback clients retain the same guard.
Positive legacy model dispatch requires current durable legacy authority.

Builder source handoff uses independently recorded user instructions and an empty
memory admission, not the memory-enriched companion brief. Personal-memory,
tone and retained artifact/status text are excluded. Native task list/check
requires exact owner/source/run joins and live status; cached completion is not
authority. Optional resumed/attached/completion carriers remain refused by C2
entry. Type/validation dependencies do not enable those optional features.

The receiving LangGraph auth implementation is included, but langgraph.json is
not switched. Internal owner-scoped clients were published separately. Source
use checks distinguish transcript use from extraction/approval/model permission.
The SQL contract harness adds current-source positive and exact-reference denial
checks against the already selected migration; no new SQL migration is included.

## Evidence and limits

93f380:1454 working-tree tests passed across MEM00, start Builder, factory flow,
vision wiring, provider fallback and task wrapper suites. Native SQL97e0d1 passed
78 checks using the selected13 migrations, including restart and duplicate
dispatch. These tests are fixtures, not hosted provider certification.

Isolated staged runtime archive959916:1454 passed, no deselections,11 warnings.
Explicit module paths exclude working-tree imports. EI925/59a8fd initially failed
23/passed1431 because the backend-only archive omitted tracked skill assets;
restoring the same candidate's skills/config.example.yaml resolved those failures
without changing runtime code. The final staged change after this run is this
evidence paragraph only.

The compiled Builder test reaches a real todo-tool round trip and second model
boundary; it does not establish complete file delivery. Current list/check tests
do not qualify every update/cancel/resume operation. No claim of ordinary Builder
full-delivery acceptance or production usefulness is made here.

Before serving activation: finish finite affected-path qualification, profile
and least-privilege grants/rollback, compatibility with current Voice repairs,
frontend integration, and the authenticated hosted lifecycle. Preserve the two
historical uncertain synthetic provider effects as pending obligations. This is
not MEMORY_TEXT_PILOT_READY and is not PROMOTE MEM00.
