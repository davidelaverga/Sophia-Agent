# MEM00 campaign state

Updated: 2026-09-05

Operational state: `R5_PROVIDER_CREDENTIAL_HOLD_LOCAL_ROLLBACK_REPAIR`. Production identity reads on 2026-09-05 report candidate `5f7f15164a708f7c6fec5a94f6ab20f707df4aa0` across frontend, Gateway, LangGraph, and Voice. The isolated remote branch remains at that commit; the reviewed observability branch remains `3add3336216e74324cd1ed4d3859caccc70c94fe`. The consecutive clean production canary count is **zero**. No promotion is claimed.

## Current verified snapshot — supersedes historical snapshots below

- DP-006 failed exact SDK deletion with the currently deployed key. Its single synthetic fixture was permanently deleted through Mem0's exact-user-filtered dashboard; independent deployed adapter pagination returned terminal count zero. Seven exact-owner database tables (candidate/canonical versions, candidates, memories, jobs, bindings, fault settings) also contain zero rows. No full production canary ran. No legacy row was deleted or imported.
- The previously created replacement key is still listed but only masked, without a reveal/recovery control. One additional same-project key-only replacement approval was requested; no response has been received. Further provider writes are paused. Current usage is 35/200000 adds and 1951/20000 retrievals on Growth Plan; no billing or provider configuration changed. Refreshed pinned LangSmith project shows no runs; the direct provider preflight has no joined trace/product metric and is non-certifying.
- EI-081 identified unsafe flag-only rollback in deployed `5f7f151`: recall-off reopens legacy search/cache and generic memory access. Both unsaved flag edits were canceled; deployed runtime/projection/fault flags remain true with canonical ownership active and an empty pool. Do not describe production recall/projection as closed. Local repair adds canonical-ownership quarantine and clears carried companion/Builder memory state; seven red regression cases are now green, focused suites passed 341 and 27 tests, and targeted lint passed. Pre-change full baseline: 5897 passed, 161 skipped. Post-change full backend suite: **5904 passed, 161 skipped, 12 warnings**, exit 0 in 291.36 seconds. The repair is locally validated and prepared for isolated publication, but remains undeployed and non-certifying.
- Subsequent same-day repair: ordinary Journal requests at 09:36:03Z and 10:51:12Z resolve to owner `sha256:3207762acc978926`. This exact owner exists in Better Auth and is distinct from the preserved VT00 principal. Both services' certification/cohort fields were corrected to this owner, verified after a full page reload, then deployed serially on exact `5f7f151`: Gateway `dep-dadva38n74is73bo42lg` became Live at 10:56:25Z and LangGraph `dep-dadvb1gn74is73bo7l30` at 10:58:16Z. Action-time Voice Lab checks at 10:55:01.821Z and 10:57:03.103Z both reported no run/operation, engaged kill switch, and closed mutation gates. No other configuration field was intentionally changed.
- Gateway's deployed shell independently confirmed exact commit, SDK `1.0.9`, corrected principal/cohort fingerprints, canonical-pool read true, and the original provider/project identity. Journal refreshed to **No saved memories yet**, matching the empty canonical database without importing or deleting legacy rows. The first form-fill/save attempt did not persist; its earlier same-commit deployment pair is explicitly non-certifying (EI-079).
- The effective deployed Mem0 key fingerprint is `sha256:109d881133f29ed5`, not the prior R3 receipt's `sha256:a489adc448f942ed`. Whitespace normalization does not explain the difference. DP-006 remeasured one isolated synthetic provider row and failed SDK deletion; exact-fixture dashboard cleanup and deployed adapter terminal zero are recorded above. No key rotation/configuration change is assumed or performed.
- Existing exact-cohort/principal/fault regressions passed 6/6 through the located `work/runtime-tools/bin/uv` environment after `uv sync --group dev`; 26 tests were deselected and eight pre-existing schema-shadowing warnings were emitted. This does not substitute for production canaries.

### Earlier same-day preflight observations (superseded where repaired above)

- The database contract is `mem00.v1`, epoch `1`, mode `enforced`, with `updated_at=2026-09-04T17:17:41.403939+00:00`. Retention values remain 30 days candidate plaintext and 3650 days content-free receipts.
- Gateway has candidate-ledger read/write, canonical-pool read, governed-runtime read, projection, fault-injection, and LangSmith-export flags enabled. Legacy inventory/import are false. Its certification principal and single-member cohort match each other at `sha256:3f1450635c581744`; this ID is **not** a `public."user"` Better Auth owner. LangGraph was previously observed with that same cohort and still needs a fresh settled read before any change.
- The previously observed ordinary Journal principal, `sha256:3207762acc978926`, exists in `public."user"`. A read-only identity join on 2026-09-05 found **no same-email link** from that owner to the configured UUID in `auth.users`; equivalence must not be assumed. Current authenticated-owner binding remains unresolved. No cohort mutation was made during this preflight.
- Structural SQL counts for both exact owner fingerprints combined are zero candidates, canonical memories, extraction runs, and projection jobs. These counts are not a complete terminal-zero cleanup certificate; bindings, faults, caches, derived state, and provider namespace must also be measured for certification.
- Fresh ordinary pending-read receipt `memrecent-20260905093447370094` returned zero rows, `source=none`, `unavailable=false`; Gateway logs joined that trace with `ownerExcluded=true`. This is not proof of the canonical path or authenticated owner. The earlier six approved rows were explicitly `local_review_overlay`, **not canonical**. The earlier 99-row Journal response is legacy-path evidence, not proof of a canonical-reader regression.
- Mem0 remains `mem0ai==1.0.9`, existing endpoint/project/organization, Growth Plan, Extra Usage off. Dashboard usage observed this day is 34/200000 adds and 1940/20000 retrievals in the 11 August–11 September window. No hosted behavior certification was run this day; previous R3 success cannot substitute for a fresh run.
- Voice Lab at `2026-09-05T09:26:58.688Z` reported no run or operation, engaged kill switch, and closed mutation gates. VT00 remains pinned to `e3b40f1b21739479db239c383ce88285ce740152`; expected-versus-observed deployment mismatch remains truthful. A new action-time check is mandatory before deployment.
- The Journal browser navigation failed with `ERR_NETWORK_IO_SUSPENDED`; the resulting built-in error page then triggered Browser Use URL-policy rejection, including an attempt to restore Settings. No bypass was attempted. User assistance to manually restore the ordinary Settings page was requested. No deployment, import, purge, provider/configuration change, or memory mutation occurred in this preflight.
- A fresh backend regression attempt stopped before collection because `uv` was absent from PATH. No bare/system-Python test was run, and no new test success is claimed.

## Historical implementation and rollout record

The following dated rollout observations are retained for audit only. Statements about absent flags, disabled schema, old deployments, or established owner equivalence are superseded by the current verified snapshot above.

## Completed safe gates

- Re-pinned repository lineage, deployed services, pre-migration schema, existing Mem0 SDK/endpoints/project/configuration/usage, and LangSmith workspace/project.
- Preserved the active VT00 worktree and reconciled every post-review commit through remote head `3add3336216e74324cd1ed4d3859caccc70c94fe`. The eight newest commits repair Voice Lab cross-region readiness, fixture accounting, synthetic cleanup/audio flushing, anonymous provider binding, finalization exit guards, durable provider cleanup, and orphan recovery; their 12-file delta does not introduce a memory authority and retains the active-run safety boundary.
- Implemented the additive durable extraction/candidate ledger, canonical authority, revisioned desired-state projection jobs/bindings, monotonic tombstones, owner-scoped governed reader, consumer convergence, identity containment, truthful privacy receipts, structural observability, and default-closed feature contract.
- The post-freeze Section 17 audit repaired the last legacy privacy routes: memory export now fails closed unless canonical and candidate-ledger scope is complete, while memory deletion rejects pending candidates, fences active and forgotten canonical rows, reports provider purge independently, and returns a typed scope-specific receipt. Gateway, LangGraph/Builder, Voice, and frontend identity endpoints now report `mem00.v1` and supported epoch `1` for mixed-version enforcement.
- The synthetic fault flag now owns a durable service-only control plane: exact certification-principal binding, one use, maximum five-minute TTL, content-free audit reference, explicit cleanup, and injected claimant/provider/database/lease/cache/LangSmith failure paths. Browser and ordinary-user identities cannot arm or consume it; the disposable proof ends with zero active fault settings.
- Pinned `mem0ai==1.0.9` in both Python package graphs and kept the existing host/endpoints/configuration.
- Verified the final migration twice in disposable PostgreSQL and executed the transaction-level contract successfully: 12 content-free governance events, two tombstones, one valid atomic prompt admission, one idempotent atomic session-finalization/extraction run, zero eligible collision bindings, two bindings held for reconciliation, and all five service-only operational views queryable.
- A final defense-in-depth audit added locked canonical scope authorization to prompt admission, explicit `scope_denied` accounting in both governed-reader passes, and content/identifier-free diagnostics across legacy Mem0 facade, gateway, retrieval, and memory middleware paths. The migration SHA-256 is `96303ed50b1508b35287cb70acd26dd34e58aa44a50b0527090f17ac302f96b7`.
- The current backend baseline remains green with 5,895 passed and 161 skipped; the focused governance/migration/consumer slice is green with 434 passed. The Voice service suite remains green with 616 passed. After reconciling remote `3add3336`, the backend capability/governance slice passed 111/111, the complete frontend suite passed 1,952 with two skips and TypeScript green, and the complete Voice Lab suite passed 348 with six skips and TypeScript green.

## Historical external-state observations (not current state)

- The exact additive production migration is applied in Supabase project `vlxnwmyvhchwbousrdzc`. Its SHA-256 was verified by full Monaco document round trip before execution. The post-check proves PostgreSQL `17.6`, `mem00.v1` epoch `1`, mode `disabled`, 13 tables, five views, 22 functions, zero browser-role table privileges, and zero active faults. No application flag or memory path is active.
- Production MEM00 flags do not exist. Signed-in environment reads showed no `SOPHIA_MEMORY_*` key in Gateway, LangGraph, Voice, or Vercel; Gateway/LangGraph retain eight legacy `MEM0_*` keys, Voice retains six, and Vercel has no `MEM0_*` or `NEXT_PUBLIC_*MEMORY*` key. A fresh read-only Supabase query pinned PostgreSQL `17.6`, 1,413 public catalog entries under the documented union, MD5 `35ae9752dee02bca116c2aa3905195f9`, zero `sophia_memory%` relations, and no `public.sophia_memory_contract`.
- Voice Lab's 2026-09-03T08:07:13Z action-time read reported no active run or operation, an engaged kill switch, and closed backend/voice mutation gates. Its frozen server/candidate remains `6f8e5180`; Gateway, LangGraph, and frontend were observed on `ce83d5a8` while Voice remained on `6f8e5180`, so Voice Lab correctly reported a deployment mismatch and left every product mutation gate closed. No voice run was started. This read authorizes no Voice Lab mutation; another action-time read remains mandatory if deployment does not immediately follow the current gate.
- The failed external LangGraph deployment is retained as evidence-bearing `MEM00-EI-009`. The YAML scanner traceback is falsified as causal because it is also present in successful deployments; the silent status-134 cause remains unproven.
- Current product deployments are converged on remote `ce83d5a8`: Gateway live deployment `dep-dacijmqfngtc73dko69g` (with a second same-commit deploy observed in progress), LangGraph `dep-dacikfuq1p3s738aih9g`, Voice `dep-dacil2qfngtc73dkslhg`, and Vercel deployment `8xF4eNvCKA4THBGHDAbNFJ2sQgU2`. None contains MEM00 code.
- The isolated branch was published at exact commit/tree `fba9e10e18870021b97055582fefc3a192947c2e` / `e0d45a0843a3882fcd5dc4281efaf7b723fb82b4` using a user-approved three-day fine-grained token restricted to `davidelaverga/Sophia-Agent` with Contents read/write; the credential was revoked immediately after verification and no pull request was created. The upstream then advanced to `fb8a6655`; that exact head was fetched, reviewed, and merged locally at pre-evidence commit/tree `53e2fc7e7a993652c1f4976311f426cb50cdc9d8` / `192c8b54da2006b707b61d3feede241f17806a3a`. The refreshed tip still requires publication before deployment. No repository merge or pull request is authorized.
- No real user has been imported or enrolled.
- The approved same-project replacement credential deleted the one fenced preflight row and then passed complete R3 on the exact deployed `mem0ai==1.0.9` runtime: three direct non-inferred rows, distinct IDs, stable get/search, page-size-one pagination, exact metadata reconciliation, repeated convergence, cross-subject isolation, exact SDK deletion, and terminal counts `[0,0,0]`. Only the existing Gateway `MEM0_API_KEY` was updated, using Render's save-only path; the prior provider key remains unrevoked for rollback and no service restart was triggered by that save.
- No Mem0 SDK/API version, endpoint, organization, project, configuration, algorithm toggle, plan, billing, or paid-service change was made. The API-key inventory now contains six hashed keys; exactly one is the approved replacement.
- The refreshed billing window remains unchanged, Growth Plan remains active, Extra Usage remains off, and post-certification usage is 34/200,000 add requests plus 1,875/20,000 retrieval requests. Projection remains closed until the additive migration and dark exact-candidate deployment are complete.
- The refreshed signed-in EU LangSmith Sophia project uses the pinned workspace/project, retains 14-day retention, and showed no runs in the selected one-day window. No trace, evaluator, automation, alert, or plan change was made.
- The isolated branch is published at `2e2de0bf797da16383109ff083c34ff852067c86` with tree `9e830f7cf941fdabc0ef5a2e439db551ff1767bd`. Render deployments `dep-dackq2u1egvs73d0ul50`, `dep-dackq6vavr4c73fjf740`, and `dep-dackqb0n74is73dhemf0` each completed successfully on that exact commit. Vercel Preview first canceled on the existing ignored-build rule and then failed closed because Preview lacks the required database TLS CA; no project setting or TLS policy was weakened. Exact production deployment `9oVBUnsFBSN2q2G2UbEry62cSsno` then built successfully and served `www.sophia-ei.com`.
- The formerly competing production campaign is now idle. The 2026-09-04T06:34:10Z Voice Lab read reported no active run or operation and an engaged kill switch. Production remains deliberately mixed and closed: Gateway `b17669ab`, LangGraph/frontend `163b5497`, and Voice `e3b40f1b`; MEM00 flags remain absent/default-false, so no memory behavior changed. The refreshed candidate is published at `ba24bef4662868609b64bc9dfaa0ca6e159f163f` / tree `1b5766c179eea2d531421ea0a6307f2bcb528d1d`.

- The resumed signed-in production re-pin confirms PostgreSQL `17.6`, `mem00.v1` epoch `1`, mode `disabled`, 13 tables, five views, 22 functions, zero browser grants, zero active faults, and zero governed principals. Mem0 remains the existing `default-project` on Growth Plan with Extra Usage off, multilingual extraction and the existing custom instructions unchanged, six hashed keys, 34 adds, and 1,933 retrievals. The EU LangSmith Sophia project retains 14-day retention and has zero runs in the selected one-day window.
- Exact candidate `ba24bef4662868609b64bc9dfaa0ca6e159f163f` is now dark-converged in production: Gateway deployment `dep-dad6him7bikc739st6m0`, LangGraph `dep-dad6hq710e5c73di51lg`, Voice `dep-dad6hrqfngtc738222j0`, and Vercel deployment `6UvzkSkbVW5SDHYcS8Y3FB7xWcDg` (public ID `dpl_6UvzkSkbVW5SDHYcS8Y3FB7xWcDg`). All four public identity endpoints report the same commit, `mem00.v1`, and epoch `1`. The action-time Voice Lab join at `2026-09-04T08:26:28Z` reported no run or operation, kill switch engaged, and product mutation gates closed. It truthfully reports an expected-identity mismatch because the preserved VT00 harness remains pinned to `e3b40f1b21739479db239c383ce88285ce740152`; no Voice Lab run was started.
- The user-designated test principal is recorded only as `sha256:3f1450635c581744`. A read-only join through the ordinary signed-in Sophia session and product database found one ordinary active owner and proved zero prior MEM00 governance rows, extraction runs, candidates, canonical memories, projection jobs, provider bindings, and active fault settings. Raw principal, email, session/token data, and memory content are excluded from evidence.
- Initial R5 configuration was bounded to that one redacted principal and the already pinned Mem0 project. Gateway reached Live as `dep-dad8nroae00c739a6mj0`; the first concurrent LangGraph restart failed on its Gateway dependency, and an accidental 3add `Deploy latest` attempt was canceled before live. Serialized exact-source deployment `dep-dad8plv10e5c73drgog0` then converged on ba24. No candidate fact or memory row had yet been created.
- The first ordinary Sophia session start failed closed with no durable session/thread/memory residue because `sophia_voice_lab_cleanup_write_fence()` treated a missing JSON synthetic marker as SQL NULL rather than false. The signed-in certification principal was independently proven distinct from the configured VT00 principal. Rollback deployments `dep-dadboef10e5c73e7neqg` (Gateway) and `dep-dadbqmgn74is73e4o6fg` (LangGraph) are Live on exact ba24 with the seven active R5 flags false; legacy gates remain false. The canonical contract is `disabled`.
- Additive repair migration `2026_09_04_voice_lab_ordinary_write_fence_repair.sql` is pinned at SHA-256 `abd7701f8effca6c1acb4034a5f2d41ee01915e923bae1e38ea0a8279cb60401`. It accepts only prior function SHA-256 `4faacbb98b20ee4e955ae8343e55c163060f9963104c384dadb1263249d28fad`, changes exactly four absent-marker classifiers to `coalesce(..., false)`, and verifies repaired SHA-256 `0678607736ee21130257e2a87f79bc807d12a0f6d22295f55079ff6bbb4aa1b2`. Production readback matched the repaired fingerprint; an ordinary session and artifact insert-update-delete transaction completed and left both probe rows zero while memory mode remained disabled. Focused frontend/Voice Lab regression is 35/35, migration file verification is exact, TypeScript is green, and lint has zero errors.

## Promotion status

`PROMOTE MEM00` is not yet permitted. Current authenticated-owner binding is proven and deployed, but the current provider credential fails deletion and the rollback safety repair is local. Required remaining work is credential authority/binding repair, complete rollback validation and immutable publication/deployment, freshly measured hosted behavior, MEM-P01–MEM-P08 certification, five consecutive clean production canaries on one immutable candidate/configuration, complete terminal-zero cleanup, and the final Section 17 evidence audit. The user authorized use of the signed-in disposable test account; this is not authorization for real-user import, bulk or ambiguous purge, project/API/SDK/plan/billing changes, or merge.
