# Davide — text memory pilot handover

**MEMORY_TEXT_PILOT_READY**, 22 September2026. Use your existing signed-in account at https://www.sophia-ei.com. The enabled owner is CUyZxRFmDNONbR0eKqkJjTrJ2z8nkDKd. Only this bounded cohort has the enabled profile; no genuine pending candidate was approved for you.

## Use

1. Start an ordinary session and select **text**. Discuss something you actually want to remember.
2. Choose **End session**, confirm, and review the recap. Keep only candidates you want saved; choose **Let it go** for the others, then **Complete**. Processing can take time; if the recap says unavailable, reload later rather than treating it as zero candidates.
3. Open **Journal** to inspect saved canonical memories. In a later conversation, ask a relevant question naturally, or say “Please search your saved memories for …”.
4. **Edit** a Journal memory to correct it. The next model admission, including the same warm conversation, checks the current revision. Indexing may lag; Sophia should not fall back to an obsolete revision.
5. **Forget** removes the memory from recall immediately and schedules exact provider cleanup. The Forgotten shelf retains a recoverable canonical record; **Restore** makes it eligible again through the governed projection path. Forget does not erase the historical conversation. Permanent deletion is a separate product action.

The two Tin Otter test records now live only on the Forgotten shelf; their known provider effects were purged. Do not restore them unless you want the disposable test data active. Your unrelated paused zero-message session and genuine pending candidates remain untouched.

## What is live

Frontend b103c4af / Production dpl_vj3NWuHsv4uf1iT5HjDJWsfLCGj8; Gateway406ff0a6; LangGraph63d9810e. Full hashes and receipts: c3-current-state.md and c3-hosted-acceptance-2026-09-22.md. Receiving auth, canonical ownership, source integrity and revocation fences remain enabled. No new recurring service was created. New acceptance spending is conservatively estimated below$2.30 against your$5 ceiling; the exact invoice total was not available.

This is an ordinary **text-memory** pilot. Legacy import, Voice personal-memory admission and Builder inherited personal memory remain outside its enabled scope. Separate Voice Lab activation/suspension remains unresolved from VT00; this mission did not alter it or demonstrate a voice journey.

## Known limits

Journal may say “Search indexing status unavailable” even when projection succeeded; that means its status view is unavailable, not that a saved memory is absent. After forget, the model correctly returned no saved memory but offered speculative scope/authorization explanations; those explanations are not reliable descriptions of the storage contract.

Five historical terminal extraction failures and two historical uncertain provider operations are retained in the evidence ledger. The new acceptance sessions/effects are independently settled; the historical records are not erased or silently marked successful. LangSmith trace coverage is degraded; actual model messages, canonical/projection receipts and final permits are available. No new automatic runs or reminders were scheduled.

## Safe disable / recovery for the operator

To pause governed recall while retaining management and cleanup, coordinate one idle shared deployment window and set **SOPHIA_MEMORY_GOVERNED_RUNTIME_READ=false** consistently on the existing Gateway and LangGraph runtime profiles. Keep candidate ledger/read, canonical Pool and provider projection intact for review and exact cleanup; keep the existing cohort/certification identities, owner declarations, receiving auth and contract epoch. Verify effective flags and the expected fail-closed result. The current governed context guard can refuse ordinary text generation while this flag is false; this is a safe pause, not a promise of seamless no-memory chat. This is a recipe, not a change performed at closure.

Do not switch off candidate writes while reads remain true, remove the projection flag while runtime reads remain true, or clear the certification cohort while the worker requires it. Do not roll back to pre-C2 code, replay migrations, reset retry budgets, strip auth or restore forgotten provider content to repair availability. If a writer itself is malfunctioning, use a separately scoped coherent flag change after accounting for pending effects.

For a problem, record the session ID, approximate time and exact symptom; preserve the recap and source records. Consult the current checkpoint and inspect the exact failed operation before retrying. Five historical exhausted targets are listed there; repeated enqueue does not create a fresh supported attempt.
