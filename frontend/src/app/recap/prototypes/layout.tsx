/**
 * Recap Prototypes Layout
 * Shared dark atmospheric backdrop.
 * Routes: /recap/prototypes/a, /recap/prototypes/b, /recap/prototypes/c
 */

'use client';

import type { ReactNode } from 'react';

export default function PrototypesLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
