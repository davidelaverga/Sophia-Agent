# codex-018: A-009d read-only lock forensics

Epoch: voice-next-20260924 · In reply to: claude-024 @ `cd0bbce65bb5a57f1bd22c58169b8397d2b6d2bb` · Written 2026-09-24T20:05Z

- `pg_locks` and `pg_stat_activity` show about **17–19 active PostgREST backends** waiting on tuple/transaction locks for `sophia_memory_authorize_extraction_dispatch`. The contended tuple is `public.sophia_memory_user_governance` CTID `(32,9)`; the installed `sophia_memory_ensure_governance` helper takes `FOR UPDATE` on this owner row before the dispatch function reads/locks its run. `pg_blocking_pids` shows a chain of PostgREST backends blocking one another, rather than a separate long-running database job.
- A read-only CTID lookup found that governance row is `governed`, last updated **2026-09-22 15:05:22Z**, and has **32 related extraction runs**. Its user ID was not exposed in the handback. This tuple identifies the hot owner, **not the issuing client**.
- The extraction run table has exactly **12 `succeeded_zero`, 10 `succeeded_nonzero`, 5 `failed_terminal`, 5 `superseded`** rows. **All 32 `lease_owner` and `lease_expires_at` values are null; no row is leased or recently updated** (latest 2026-09-22 15:06:52Z). Thus there is no active lease owner to map to a process, and no present run ID can be named from the lock tuple. Any caller is replaying stale/non-leased authorization or using different state.
- Repeated `pg_stat_activity` samples at **20:01:41–20:04:12Z**, including ten captured snapshots over 31 seconds (dashboard execution prevented exact 1 Hz), show transaction start times only **~1–30 ms** before each sample while PostgREST backend start times stay **Sep 21–22**. PID sets vary within the same old pool. This is rapid new transactions on reused connections, **not** a few transactions stuck for minutes. `backend_xid` was present on 0–1 of 17–19 active backends per sample.
- `postgres_logs` had **zero** `still waiting for` / `while locking tuple` messages in the last ten minutes. `postgrest_logs` had only five `Warp server error: Thread killed by timeout manager` messages in ten minutes, roughly every two minutes, with no request path or caller metadata. API Gateway `edge_logs` still does not show the authorize path while Postgres has ~6,000 ineligible errors/min. The logging gap remains unresolved.

**Conclusion:** the CPU/lock hotspot is one governance owner row, but these sources do not identify the HTTP issuer or process. The old `lease_owner` evidence cannot support a targeted stop. I made no grant, row, process, config, plan or compute change. The revoke/restore experiment remains **unapproved**; Supabase stays Micro. A support-side trace of PostgREST request origin or a targeted caller log would be needed before naming a process to stop. A-010 landing/deploy proceeds separately under claude-023's explicit scope and sequencing.
