# A-013 single voice validation run packet — preparation only

Prepared 2026-09-24 23:10 UTC. No provider run, deployment, sizing, gate or environment change was made in this preparation phase. The A-010 authorization is one ordinary-app run, at most two pinned non-Builder utterances, a verified hard 300 s run cap, and US$3.25 additional all-in spend. The run remains held until the post-retention gates below pass. The R1–R3 window showed no product repair; C5 acceptance is historical.

## Exact services and baseline

| Service | Render ID | 2026-09-24 23:10 UTC state | Current source / deployment | Prior rollback reference |
| --- | --- | --- | --- | --- |
| Lab worker | `srv-da6uiqfavr4c739mtbo0` | Live, one instance, Starter 0.5 CPU/512 MiB, kill=true | `d467ab97464908b4e7c7752701eee9d24db7faf6`; `dep-daqgk30473hc7383cle0` | preceding successful same-source deploy `dep-daqggmojo6nc73ec8gs0`; for W1 rollback, redeploy this known prior source and keep gates closed |
| Lab MCP | `srv-da6uiqfavr4c739mtbng` | Suspended, zero instances, Starter | `d467ab97464908b4e7c7752701eee9d24db7faf6`; `dep-daqgfrm7bikc738efng0` | preceding successful same-source deploy `dep-daqgccqd0e5s73ag2alg` |

Tomorrow's W1 source is the exact `codex/vt00-c5-first-use-repair` commit `eb849b62d0e80777fbe1f333818510b0e7f1ff7f`. Claude checked `tools/sophia-voice-lab` against reviewed W1 merge `30b11147618cba4c89efe147cc25aade1634ab5b`: empty diff. Deploy that exact commit to both existing Lab services; do not sync the Blueprint to Pro or create a service. Preserve environment and schema. Before each deploy record current deployment and rollback ID again, since this packet is a point-in-time snapshot.

Gateway `srv-d7be5s9r0fns7397l4g0` was last read back Live at `eb849b62` / `dep-daqp83hsrm7s73dttcmg`, with `SOPHIA_VOICE_LAB_ENABLED=false` and `SOPHIA_VOICE_LAB_KILL_SWITCH=true`; previous rollback deployment `dep-daqo76mk1f9s73csgeig`. Frontend Production `www.sophia-ei.com` was Ready at `12ce0f8981f89d97ec96e2549e497442f2b61613` / `dpl_GPyryqPJ9GJaSEk25Kk1Zej7CEex`; preceding rollback `dpl_9At5rgZBDQpXew6ePeMGRwvRvxnD`. Voice and LangGraph served identities must be freshly read from capabilities before admission. Treat source SHA and deployed SHA as different facts.

## Cap and cost baseline

- Set `SOPHIA_VOICE_LAB_MAX_RUN_SECONDS=300` on the W1 Lab deployment tomorrow. Read it back from Render environment and from `get_capabilities` as effective `max_run_seconds=300` before admission. A read-only `get_capabilities` tonight returned MCP internal error while suspended; no effective 300 s claim is made yet.
- Render September month-to-date at approximately 23:10 UTC: Lab worker **$5.77** (including historical Starter and Pro charges), Lab MCP **$5.18** (suspended), combined **$10.95**. Billing values may lag; capture new baselines just before service changes and after cleanup. These totals are not A-010 campaign charges.
- Google AI Studio project Sophia, September 1–24 month-to-date: **€0.17** Gemini API cost, all keys and models, observed around 23:10 UTC. The dashboard warns reporting may lag up to 24 h. Re-read before and after; do not convert this historical MTD value into an incremental campaign charge without timestamps and exchange-rate basis.
- Conservative A-010 budget envelope: provider $1.78 + Pro 3 h $0.38 + MCP/redeploy $0.10 + retention $0.30 + cleanup reserve $0.50 = **$3.06**, leaving $0.19 under **US$3.25**. Stop if projected additional all-in cost exceeds $3.25; no paid retry.
- Re-project this envelope after step 4 using the actual Pro start time, and again immediately before step 8. Either projection above US$3.25 aborts admission; the $0.19 initial headroom is not a waiver.

## Tomorrow's ordered gates and readbacks

1. At or after **2026-09-25 11:19:18.814 UTC**, let the existing `sophia-voice-a-007-final-purge-and-suspend` automation perform final R1/R2/R3 purge proof and service suspension. It is ACTIVE; its prompt schedules first action at or after **11:25 UTC**. Require terminal run state, exact local and remote purge fields, settlement receipt hashes, zero active runs/leases, and both Lab services at zero instances. If proof is incomplete, hold and preserve the automation.
2. Only after that readback, run `VACUUM (ANALYZE)` on the separate `sophia-voice-lab-postgres`; record dead tuples and database size before/after. Do not delete retained J4/J5/J6/C5 data or rows outside retention. No `VACUUM FULL`, `REINDEX`, `CLUSTER`, compute or schema change.
3. Record service tuples, resume/deploy exact W1 `eb849b62` to Lab MCP and the existing worker, and read back successful deployments, one worker and clean leases. Do not open provider admission yet.
4. Change **only the existing worker** from Starter to Pro 2 CPU/4 GiB in Render Compute. Set the 300 s cap, keep one instance and other settings, and read back live cgroup CPU quota **≥2 CPU** and memory limit **≥3.5 GiB**. Require `get_capabilities` to report the 300 s cap and exact deployed tuple. A dashboard plan label alone is insufficient.
5. Verify the **served** frontend's signed `auth:readiness` response has `control_adapter_enabled=true`, exact Production deployment identity, and no timeout. If enabling the frontend adapter is required, rebuild the same approved frontend source with the adapter enabled while product admission remains closed; record the new deployment ID and the W3 rollback target `dpl_GPyryqPJ9GJaSEk25Kk1Zej7CEex`, then recheck the served deployment. Stop if the effective adapter cannot be proven. Also recheck Gateway, Voice, LangGraph and Lab identities, clean leases and retained obligations.
6. In Supabase production, take two `pg_stat_statements` observations 60 s apart. The authorized load rule is delta total execution time **<0.5 s/s** with no renewed dispatch storm. Hold if it fails or telemetry is unavailable. Keep Supabase Micro.
7. Only after steps 1–6 pass, open the minimum required frontend, Gateway, Voice and Lab flags on their existing services: `SOPHIA_VOICE_LAB_ENABLED=true`, `SOPHIA_VOICE_LAB_KILL_SWITCH=false`, and frontend `SOPHIA_VOICE_LAB_CONTROL_ADAPTER_ENABLED=true` where applicable. Resume MCP if needed. Wait for every deployment to be Live/Ready and read back effective values through Render/Vercel and `get_capabilities`. Do not accept a source-branch flag as an effective gate readback.
8. Start **one** ordinary-app non-Builder run through Sophia Voice Lab MCP. Speak pinned `conversation_greeting_probe`, then pinned `conversation_calm_probe` only after observing the first turn. Verify PCM receipt, browser-time continuity, provider input transcript and reply separately. Use supported End, await canonical finalization, export and settlement. Recovery settlement is not normal End. Do not label invalid delivery a product failure.
9. Close admission: restore `SOPHIA_VOICE_LAB_ENABLED=false`, kill switch `true` on all affected planes. Restore an **adapter-disabled Production frontend deployment** (using the recorded W3 rollback target if applicable) and read back `control_adapter_enabled=false` from the served site's signed readiness; record its deployment ID. Verify effective closed gates and zero active runs/leases. Return the same worker to Starter 0.5 CPU/512 MiB with kill=true and suspend both Lab services once retention duties are safely assigned; verify zero instances.
10. Read back incremental Render and AI Studio cost and preserve the new run's retention deadline, evidence references and cleanup mechanism.

Hard aborts: wrong deployed identity, unsatisfied signed adapter preflight, worker profile below 2 CPU or 3.5 GiB, cap not 300 s, active run/lease, stale purge proof, untrusted/missing browser capture time, input gap >150 ms, lag growth >100 ms or context/wall ratio error >5%, provider/harness auth or evidence abort, load ≥0.5 s/s or storm, failed readback, and projected incremental cost >US$3.25. Stop and report with no retry. A failure after admission still requires supported End/recovery and cleanup. A single scoped observation is the target, not VT00 certification.
