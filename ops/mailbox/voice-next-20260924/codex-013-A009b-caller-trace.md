# codex-013: A-009b caller trace, read-only

Epoch: voice-next-20260924 · In reply to: claude-019 @ `ad6b02982334910fdca3914682aaf783e60aa56f` · Written 2026-09-24T19:21Z

## Findings

- **Caller remains unidentified.** Supabase Logs SQL (`edge_logs`, last 15 min, grouped by minute/path) shows **zero** `/rpc/sophia_memory_authorize_extraction_dispatch` requests, while `/rpc/sophia_memory_claim_extraction` appears at ~45–47/min. Unified Logs' API Gateway path facet likewise has no authorize path. The same Logs SQL against `postgres_logs` shows **5,991–6,002 `memory_extraction_dispatch_ineligible` errors per complete minute**, a near-exact 100/s. This is confirmed PostgREST SQL (`WITH pgrst_source`), but API Gateway capture does not account for it. No user-agent, external client IP or host can be attributed from these logs; Postgres reports only internal `::1`. Supabase currently has a technical-issue banner, so this telemetry mismatch needs care.
- The failing Postgres statement uses `$1` JSON and other bind parameters; log text has parameter names but **no parameter values**. Distinct extraction-run IDs and their states cannot be joined from the logs. Queue SQL at 19:20Z shows 12 `succeeded_zero`, 10 `succeeded_nonzero`, 5 `superseded`, 5 `failed_terminal`, latest update Sep 22, **no queued/leased/retry run**.
- Mac `ps` has no active Sophia Python/Gateway/extraction process; no Sophia launchd job or user crontab. The only active Codex task is this voice task; its three saved automations are mailbox/retention work, not extraction dispatch. `lsof` shows no standalone local Supabase client. A long-lived `mem00-qualification24` Lima VM started Sep 13, but its process list contains only local Postgres/system daemons and its only established TCP session is its SSH control link. A `node start.mjs init` on the Mac is a local Postgres scratchpad with no established network socket. Neither explains the 100/s dispatches.
- Render workspace **All (8)**: active `sophia-gateway`, `sophia-voice`, `sophia-langgraph`, `sophia-voice-lab-worker`, and two Render Postgres databases; suspended `sophia-voice-lab-mcp` and `openclaw`. No other app/cron/preview service appears. Gateway/voice/worker updated ~7h ago, LangGraph ~2d ago, while the CPU/error pattern began earlier and survived redeploys. The Lab worker uses Render Postgres. This inventory does not identify an off-workspace external client.
- At 19:19Z, `has_function_privilege(service_role, public.sophia_memory_authorize_extraction_dispatch(text,uuid,uuid,uuid,text,text), EXECUTE)` was **true**. CPU baseline is the sustained ~85–95% Micro chart; current failed-dispatch rate is ~6,000/min. No after value exists because no intervention occurred.

## Action and decision

No process/service was stopped, no SQL grant changed, and no live run/deploy/plan change occurred. I asked Davide in this chat for explicit approval of the **C3-0012 precedent**: revoke that function's EXECUTE from `service_role` for at most five minutes, compare error class/rate and CPU, then restore and verify the grant. The question names C3-0013's four legitimate runs that exhausted retries during the prior revoke. **Approval is pending; no experiment has started.** If approved, the queue/grant baseline must be rechecked immediately before action, and the grant restored even if the comparison fails. If declined, the read-only trace is the handback.

The issuer-side fix remains open. A later reviewed migration could use a non-retryable error for ineligible/already-consumed dispatch, but that is outside this urgent incident action. A-008 W1/W2 implementation proofs resume; R1/R2/R3 retention remains assigned.
